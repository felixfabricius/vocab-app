import { Card, Screen } from "@/app/components/ui";

/** Placeholder until milestone 2 adds photo, paste, text and translate-log imports. */
export function ImportScreen() {
  return (
    <Screen title="Import">
      <Card>
        <p className="text-muted">Card creation arrives in the next milestone: photo, paste from the Claude app, text and subtitle files, translate log.</p>
      </Card>
    </Screen>
  );
}
